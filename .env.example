# ═══════════════════════════════════════════════════════════════════════════
# Vanmoer Platform — environment configuration
# Copy this file to `.env` and fill in real values. `.env` is gitignored and
# must never be committed — this file (.env.example) is the one that is.
# ═══════════════════════════════════════════════════════════════════════════

# ── Gemini (Google Generative AI) ────────────────────────────────────────────
# Used for LLM document extraction (helpers/gemini_client.py, and directly in
# clients/carpenter/inbound/arrival_notice.py + scanned_doc.py).
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key-here
GEMINI_MODEL=gemini-2.5-flash-lite

# ── Auth (JWT) ───────────────────────────────────────────────────────────────
# JWT_SECRET signs every login token (helpers/jwt_utils.py). Use a long random
# string in any real environment — the app falls back to an insecure default
# if this is unset, which is only acceptable for local scratch testing.
# Generate one with: python -c "import secrets; print(secrets.token_hex(32))"
JWT_SECRET=change-me-to-a-long-random-string
JWT_EXPIRY_HOURS=12

# ── Server ───────────────────────────────────────────────────────────────────
# FLASK_DEBUG=1 runs Werkzeug's dev server + reloader (LAN-reachable on
# 0.0.0.0). Set to 0 for a real deployment — main.py then serves through
# waitress (a proper multi-threaded WSGI server) instead.
FLASK_DEBUG=1
PORT=5000
# Only used when FLASK_DEBUG=0 (waitress worker thread count).
WAITRESS_THREADS=16

# ── Backups ──────────────────────────────────────────────────────────────────
# How often database/backup.py's scheduled job snapshots app.db -> app_backup.db,
# then uploads both to Google Drive (database/drive_sync.py), overwriting the
# same two Drive files in place each run. Drive upload requires credentials.json
# (OAuth client secret) and a one-time `python -m database.drive_auth` to create
# token.json — see database/drive_auth.py.
BACKUP_INTERVAL_HOURS=1

# ── Database (optional overrides) ───────────────────────────────────────────
# Only needed if you want the SQLite files somewhere other than database/.
# Leave unset to use the defaults: database/app.db and database/app_backup.db.
# DATABASE_PATH=database/app.db
# DATABASE_BACKUP_PATH=database/app_backup.db

# ── ITOS screenshot automation (helpers/itos_automation.py) ────────────────
# Drives an on-screen Edge window inside a Windows 365 remote session to
# screenshot iTOS orders — only runs on Windows, with a real desktop session
# available (a real logged-in, unlocked screen — this cannot run as a
# background service or a "run whether user is logged on or not" task).
#
# The 5 values below are REQUIRED and specific to THIS PC's Windows App
# install and screen layout — there is no safe value to ship as a code
# default (a stale default copied from a different machine is exactly what
# silently clicked the wrong spot before), so the app raises a clear error
# at request time if any of them are missing, naming exactly which one.
#
# Find ITOS_APP_ID by running this in PowerShell on this PC:
#   Get-StartApps | Where-Object { $_.Name -like "*Windows*365*" -or $_.Name -like "*Windows App*" }
# Find the 4 coordinates by opening Windows App manually on this PC once and
# noting the pixel position of the Apps tab and the Edge card/tile.
# Full walkthrough: README.md "ITOS screenshot automation setup".
ITOS_APP_ID=MicrosoftCorporationII.Windows365_8wekyb3d8bbwe!Windows365
ITOS_APPS_TAB_X=301
ITOS_APPS_TAB_Y=347
ITOS_CARD_X=410
ITOS_CARD_Y=457

# Optional — safe cross-machine defaults, not specific to this PC.
# ITOS_TARGET_URL=https://itos.vanmoer.com
# ITOS_INDEX_URL=https://itos.vanmoer.com/Stevedoring/Order/Index
# ITOS_MAX_LAUNCH_RETRIES=3
# How far back (in years, from today) the iTOS index page's "Order Date
# From" filter is set on every search, so old orders aren't hidden by the
# page's default date range.
# ITOS_DATE_FROM_YEARS_BACK=5
# How often the single background worker (helpers/screenshot_worker.py)
# polls the queue for the next job when idle.
# SCREENSHOT_POLL_SECONDS=3

# ── Outlook forward-with-screenshots automation (helpers/outlook_automation.py) ──
# Drives its OWN isolated Chrome instance (Playwright) against Outlook Web —
# unlike the ITOS automation above, this does NOT need the physical desktop,
# but login still goes through Microsoft's phone-call MFA challenge. A saved
# browser session (in OUTLOOK_PROFILE_DIR, already set up) keeps this
# unattended for about 20 days at a time ("Don't ask again for 20 days");
# when it expires, a job fails with "Outlook session expired — needs manual
# re-login" and a human answers the phone once on this machine to refresh it.
# Full walkthrough: README.md "Outlook email automation setup".
#
# OUTLOOK_EMAIL/OUTLOOK_PASSWORD are only used on a fresh/expired-session
# login attempt — day-to-day runs reuse the saved profile and never touch
# these. Required regardless, so a re-login can be attempted automatically
# up to the "Sign in" step (the MFA phone call itself still needs a human).
OUTLOOK_EMAIL=your-outlook-account@vanmoer.com
OUTLOOK_PASSWORD=change-me
# Comma-separated — every approved forward goes to ALL of these, every time.
OUTLOOK_FORWARD_TO=recipient1@vanmoer.com,recipient2@vanmoer.com

# TESTING SWITCH — defaults to false (dry run): every real step runs
# (search, open thread, Forward, fill To, paste all 3 screenshots inline),
# but the Send button is never clicked, and the row still gets marked
# "sent" so you can verify the automation mechanics without emailing
# anyone. Set to true only once you're ready for real sends.
OUTLOOK_SEND_ENABLED=false

# OUTLOOK_PROFILE_DIR defaults to <this project>/outlook_profile/ — a
# one-time copy of the completed-MFA session Screenshot/outlook.js
# originally used, kept inside this project's own source directory (it's
# gitignored — never committed, it holds live session cookies). Only
# override if this folder or Chrome's install path ever moves.
# OUTLOOK_PROFILE_DIR=outlook_profile
# OUTLOOK_CHROME_PATH=C:\Program Files\Google\Chrome\Application\chrome.exe

# Optional text inserted at the top of the forwarded message body, above the
# pasted screenshots. Leave unset for no intro text.
# OUTLOOK_FORWARD_INTRO_TEXT=
# How many times the phone-call MFA challenge is retried (re-clicking Call
# via "Sign in another way") before giving up for this attempt
# (LoginRequiredError) — each attempt gives the phone up to 2 minutes to
# ring and be answered. Never blocks the worker indefinitely.
# OUTLOOK_MFA_MAX_RETRIES=3
# How often the email worker (helpers/email_worker.py) polls the queue for
# the next job when idle.
# EMAIL_POLL_SECONDS=3
